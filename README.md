# GUI for the BOLD simulation of the Modular Smartphone Scenario

Requires the BOLD dump files being served at
```
http://localhost:8000/modularSmartphone-${i}.trig
```

## Start
```
npm install
npm start
```